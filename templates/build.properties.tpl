name        = 
version     = 1.0.0
final.name  = ${name}-${version}

src                = ${basedir}/src
java.src           = ${src}/java
test               = ${basedir}/test
lib                = ${basedir}/lib
build              = ${basedir}/build
build.classes      = ${build}/classes
build.test.classes = ${build}/test.classes
build.unjar        = ${build}/unjar
build.src          = ${build}/src
resource           = ${basedir}/resource
dist               = ${basedir}/dist
reports            = ${basedir}/reports
reports.html       = ${reports}/html
